{
  "name": "pestoverse-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler publish",
    "dev": "wrangler dev --experimental-local --persist"
  },
  "devDependencies": {
    "@miniflare/tre": "^3.0.0-next.13",
    "wrangler": "^3.20.0"
  }
}
